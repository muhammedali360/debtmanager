.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store

# Local user database — never commit real financial data
data/
*.db
.streamlit/secrets.toml

# tools/screenshot.mjs needs Playwright; the install is local, not vendored.
# No trailing slash: pointing node_modules at a shared install makes it a
# symlink, which git treats as a file, and "node_modules/" does not match it.
node_modules
